$(document).ready(function(){

  $("#static_home_slider").royalSlider({
      // options go here
      // as an example, enable keyboard arrows nav
      keyboardNavEnabled: true,
    	autoPlay: {
    		// autoplay options go gere
    		enabled: true,
    		pauseOnHover: true
    	}
  });
	
});