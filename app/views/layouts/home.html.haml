!!!
%html
  %head
    %title= full_title(yield(:title))

    = stylesheet_link_tag    "application", media: "all"
    = javascript_include_tag "application"

    = yield :javascript_includes

    = csrf_meta_tags
    %meta{charset: "UTF-8"}/
    %meta{content: "notranslate", name: "google"}/
    %meta{content: "en", "http-equiv" => "Content-Language"}/

    /[if lte IE 9]
      = javascript_include_tag "http://html5shim.googlecode.com/svb/trunk/html5.js", media: 'screen', rel: 'stylesheet'

    = render 'shared/google_analytics'

  %body{:class => ("signedout" if !signed_in? || params[:action] == "complete_profile" || params[:back_url] == "/account/complete_profile") }
    = render 'layouts/header'

    = render 'layouts/flash', flash: flash

    #application_container.regular
      = yield
  = render 'shared/modal_help' if signed_in?
  = render 'shared/modal_contact'