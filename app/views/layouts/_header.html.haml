%header.navbar.navbar-inverse.navbar-fixed-top
  .navbar-inner{:class => ("signedout" if !signed_in?) }
    .container
      #header_logo_nav{:class => ("signedout" if !signed_in?) }
        = link_to image_tag("app/logo.png"), root_path

      - if signed_in?

        #application_notify

        - if current_user.completed?
          %ul#header_nav_links_left.nav.pull-left

            %li.divider-vertical
            %li
              #header_nav_panel_browse.panel.gradient
                %ul
                  / MAP: pushpin or globe or picture or road or map-marker
                  %li#header_nav_button_container_browse
                    = link_to raw("<i class='icon-pushpin icon-large icon-white'></i> Browse"), map_path
                  %li
                    = form_tag '#', method: 'get', id: "header_nav_panel_browse_search_form", onsubmit: 'return false' do
                      = text_field_tag :search, params[:search], placeholder: "Search for users...", id: "header_nav_panel_browse_search_field", class: 'search-query'

            %li
              = link_to raw("<i class='icon-random icon-large icon-white'></i> Library"), library_path


            %li.divider-vertical
            %li.header_nav_icon_narrow
              = link_to raw("<i class='icon-info-sign icon-large icon-white'></i>"), '#', id: 'header_link_help'
            %li.header_nav_icon_narrow
              = link_to raw("<i class='icon-phone icon-large icon-white'></i>"), '#', id: "header_link_contact"


          %ul#header_nav_links_right.nav.pull-right

            = render 'header/menu_notifications'

            = render 'header/menu_profile'

        - else

          %ul.nav.pull-right

            = render 'header/menu_profile_incomplete'

      - else
        %ul#header_nav_links_left.nav.pull-left

          %li.divider-vertical

        = render 'header/signin_form'