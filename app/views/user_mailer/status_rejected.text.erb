<%= "Dear #{@user.first_name}" %>

Your status was rejected by an administrator.

Before you can be contacted by other users, your status must be approved. Sign in below to ammend it:

<%= root_url %>
