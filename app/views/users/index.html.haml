- provide(:title, 'Map')

= content_for :content do

  / yet to find a way to get these user details in users.js.erb...
  / how can I call the .ready function outside of the view!!
  :javascript
    var user_lat = "#{current_user.lat}";
    var user_lng = "#{current_user.lng}";

    $(function() {
      users_index_loadmap();
    });

  #users_index_map.map

  #users_index_mapcontainer_users.mapcontainer
    .panel
      Users

  #users_index_mapcontainer_user.mapcontainer
    #users_index_user
      User