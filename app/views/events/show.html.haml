%h1 Appointment with #{@event.partner.name}
%p.info
  Published on #{@event.datetime.strftime("%B %e, %Y")}
%p
  = link_to "Edit event", edit_event_path(@event)
  |
  \#{link_to "Back to events", events_path}