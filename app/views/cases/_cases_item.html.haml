.row.cases_index_cases_item{"data-id" => cases_item.id.to_s }
  .span4
    .cases_avatar.index
    = avatar_for(cases_item.interviewer, 'icon')
  .span5.cases_index_item_scoreandtime
    .cases_score
      = cases_item.totalscore
    .row.cases_index_timestamp
      = cases_item.created_at.strftime("%d %b '%y")
  .span15
    .row
      .cases_index_name
        = cases_item.interviewer.name
    .row.font-small
      = cases_item.subject_trunc